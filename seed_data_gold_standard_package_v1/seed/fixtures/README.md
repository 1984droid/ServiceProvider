# Django Fixtures (Optional)

If you prefer `manage.py loaddata`, you can convert the seed_plan into fixture JSON/YAML.

Because your schema is evolving, the script approach is usually faster and safer.

When stable:
- export fixtures from dev DB
- keep them versioned here
